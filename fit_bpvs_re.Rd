% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bpvs_model.R
\name{fit_bpvs_re}
\alias{fit_bpvs_re}
\title{Fit BPVS with random intercepts (BPVS-RE)}
\usage{
fit_bpvs_re(
  formula,
  data,
  id_var = "id",
  chains = 4,
  iter = 4000,
  warmup = 2000,
  cores = 4,
  seed = 123,
  adapt_delta = 0.95,
  max_treedepth = 12,
  refresh = 500,
  backend = NULL,
  ...
)
}
\arguments{
\item{formula}{A linear formula of the form \code{y ~ x1 + x2 + ... + xp}.}

\item{data}{A data frame containing the variables in \code{formula} and the
unit identifier \code{id_var}.}

\item{id_var}{Name of the column holding the cross-sectional identifier.}

\item{chains, iter, warmup, cores, seed}{Number of chains, iterations per chain,
warm-up draws, cores, and RNG seed passed to \code{\link[brms]{brm}}.}

\item{adapt_delta, max_treedepth}{NUTS sampler controls passed to
\code{\link[brms]{brm}}.}

\item{refresh}{MCMC progress print interval passed to \code{\link[brms]{brm}}.}

\item{backend}{Stan backend, see \code{\link{bpvs_backend}}. Auto-detected
when \code{NULL}.}

\item{...}{Additional arguments passed to \code{\link[brms]{brm}}.}
}
\value{
A fitted \pkg{brms} model object (class \code{brmsfit}) with
  attributes \code{bpvs_effects}, \code{bpvs_slopes}, and
  \code{bpvs_id_var} attached.
}
\description{
Estimates the Bayesian Panel Variable Selection model in which unit-specific
intercepts are exchangeable random effects, \eqn{\alpha_i \sim N(0,
\tau_{\text{unit}}^2)}{alpha_i ~ N(0, tau_unit^2)}, and the regularized
horseshoe prior is applied to the slopes.
}
\details{
The model is estimated as \code{y ~ x1 + ... + xp + (1 | id)}.
}
\seealso{
\code{\link{fit_bpvs_fe}} \code{\link{bpvs_pipeline}}
}
