% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bpvs_simulation.R
\name{simulate_panel_data}
\alias{simulate_panel_data}
\title{Simulate panel data with known coefficients}
\usage{
simulate_panel_data(
  N_units = 50,
  T_periods = 10,
  beta_true = c(1.5, -0.8, 0, 0.5, 0, 0),
  sigma = 0.5,
  tau_unit = 0.4,
  rho = 0.3,
  predictors = NULL
)
}
\arguments{
\item{N_units}{Number of cross-sectional units.}

\item{T_periods}{Number of time periods per unit.}

\item{beta_true}{Numeric vector of true slope coefficients.}

\item{sigma}{Idiosyncratic error standard deviation.}

\item{tau_unit}{Standard deviation of the unit random intercepts.}

\item{rho}{Autocorrelation of the Toeplitz predictor covariance.}

\item{predictors}{Optional character vector of predictor names; defaults to
\code{paste0("x", 1:p)}.}
}
\value{
A data frame with columns \code{id}, \code{year}, \code{Income}
  (the outcome), and one column per predictor.
}
\description{
Generates a balanced panel with unit-specific random intercepts and
correlated Gaussian predictors, so that the true coefficients (including
zero entries) are known. Useful for validating parameter recovery and
selection performance.
}
\details{
The design matrix is drawn from
\deqn{\mathbf{x}_{it} \sim N(\mathbf{0}, \mathbf{\Sigma}),
\quad \mathbf{\Sigma}_{ij} = \rho^{|i-j|},}{x_it ~ N(0, Sigma),
Sigma_ij = rho^|i-j|,}
i.e. a Toeplitz autocorrelation structure, and the outcome is
\deqn{y_{it} = \alpha_i + \mathbf{x}_{it}^\top \boldsymbol{\beta}
+ \varepsilon_{it},}{y_it = alpha_i + x_it' beta + eps_it,}
with \eqn{\alpha_i \sim N(0, \tau_{\text{unit}}^2)}{alpha_i ~ N(0,
tau_unit^2)} and \eqn{\varepsilon_{it} \sim N(0, \sigma^2)}{eps_it ~ N(0,
sigma^2)}.
}
