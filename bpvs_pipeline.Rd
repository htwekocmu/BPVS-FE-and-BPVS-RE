% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bpvs_diagnostics.R
\name{bpvs_pipeline}
\alias{bpvs_pipeline}
\title{Fit, rank, and compare BPVS models (pipeline)}
\usage{
bpvs_pipeline(
  formula,
  data,
  effects = c("fe", "re"),
  outcome_var = "Income",
  id_var = "id",
  time_var = "year",
  chains = 4,
  iter = 4000,
  warmup = 2000,
  cores = 4,
  seed = 123,
  adapt_delta = 0.95,
  max_treedepth = 12,
  ...
)
}
\arguments{
\item{formula}{A linear formula of the form \code{y ~ x1 + x2 + ... + xp}.}

\item{data}{A data frame containing the variables in \code{formula} and the
unit identifier \code{id_var}.}

\item{effects}{Character vector of specifications to fit; any subset of
\code{c("fe", "re")}.}

\item{outcome_var}{Name of the outcome column.}

\item{id_var}{Name of the column holding the cross-sectional identifier.}

\item{time_var}{Name of the time identifier (for the frequentist Hausman
test).}

\item{chains, iter, warmup, cores, seed}{Number of chains, iterations per chain,
warm-up draws, cores, and RNG seed passed to \code{\link[brms]{brm}}.}

\item{adapt_delta, max_treedepth}{NUTS sampler controls passed to
\code{\link[brms]{brm}}.}

\item{...}{Additional arguments passed to \code{\link[brms]{brm}}.}
}
\value{
An invisible list with elements (as available):
  \code{fit_fe}, \code{fit_re}, \code{rank_fe}, \code{rank_re},
  \code{pip_fe}, \code{pip_re}, \code{stats_fe}, \code{stats_re}, and
  \code{comparison}.
}
\description{
One-call driver: fits BPVS-FE and/or BPVS-RE, computes posterior inclusion
probabilities, ranks the predictors, extracts fit statistics, prints the
results, and (when both specifications are fitted) computes the Bayesian and
frequentist Hausman comparison.
}
\examples{
\dontrun{
# Quick demo on synthetic data (small MCMC, for illustration only)
set.seed(456)
dat <- simulate_panel_data(N_units = 20, T_periods = 6,
                           beta_true = c(1.5, -0.8, 0),
                           sigma = 0.8, tau_unit = 0.4)
fmla <- Income ~ FDI + INF + POP
res <- bpvs_pipeline(fmla, dat, effects = c("fe", "re"),
                     outcome_var = "Income", chains = 2,
                     iter = 500, warmup = 250, cores = 1, seed = 1)
}
}
